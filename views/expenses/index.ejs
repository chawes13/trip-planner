<% include ../partials/header %>

<div class="container">
    <h1>Expenses</h1>
    <% if(expenses && expenses.length > 0){ %>
        <table class="table table-striped">
            <thead>
                <th>Description</th>
                <th>Amount</th>
                <th>Created By</th>
            </thead>
                <% expenses.forEach(function(expense){ %>
                    <tr>
                        <td><%= expense.description %></td>
                        <td>£<%= expense.txnAmount %>.00</td>
                        <td><%= expense.createdBy %></td>
                    </tr>
                <% }); %>
        </table>
    <% } %>
    <a href="/trips/<%= trip_id%>/expenses/new" id="newExpense" class="btn btn-success">Create New Expense</a>
</div>

<% include ../partials/footer %>